{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "delete-schemas": "cd scripts && clean-schemas.bat",
    "create-schemas": "cd scripts && create-schemas.bat"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "ajv": "^8.10.0",
    "typescript-json-schema": "^0.53.0"
  }
}
